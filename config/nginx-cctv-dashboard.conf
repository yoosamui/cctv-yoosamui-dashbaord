server {
    listen 8880;
    server_name _;

    root /home/pi/cctv/cctv-dashboard;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /assets/ {
        access_log off;
        expires 7d;
    }

    error_page 404 /index.html;
}
